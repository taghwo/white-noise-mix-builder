node_modules
dist
*.log

# Sounds: keep only the manifest. The white/pink/brown noise loops are
# regenerated by `npm run gen-sounds`, and any other audio dropped in here is
# treated as local/user content (don't commit large or unlicensed files).
assets/sounds/*
!assets/sounds/manifest.json
.env